XrayNotes Plugin 

Convert current image to Grayscale.

This DLL uses the macro

// This is avaraging the 3 colors
#define xRGB2GRAY(r,g,b) ( (((int)(r)) + ((int)(g)) + ((int)(b)) ) / 3   )

See plugbw for an alternative gray scale routine.


